<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunny.mappers.Admin_Mapper">
	<!-- 관리자 로그인 -->
	<select id="adminLogin" parameterType="AdminDTO" resultType="AdminVO">
		select admin_id,admin_pw,admin_lastdate,admin_name 
		from admin_tbl
		where admin_id=#{admin_id} and admin_pw=#{admin_pw}
	</select>
	<!-- 관리자 로그인 시간 업데이트 -->
	<update id="adminLoginTime" parameterType="String">
		update admin_tbl 
		set admin_lastdate = sysdate
		where admin_id=#{admin_id}
	</update>
</mapper>